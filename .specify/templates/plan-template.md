# {Demo Name} - Technical Architecture Plan

> Generated by `/speckit.plan` from `spec.md`

## Overview

| Attribute | Value |
|-----------|-------|
| **Spec Reference** | `specs/{demo-slug}/spec.md` |
| **Zone Architecture** | {Zone A Only / Zone B Only / Both Zones} |
| **Primary Pattern** | {A: CRUD / B: Analytics / C: RAG / D: Streaming} |
| **Cortex Features** | {Analyst, Agent, Search, LLM Functions} |
| **Multi-Tenant** | {Yes / No} |
| **Created** | {Date} |

---

## Zone Architecture Decision

### Selected: {Zone A Only / Zone B Only / Both Zones}

**Rationale**: {Why this zone configuration was chosen}

```
┌─────────────────────────────────────────────────────────────┐
│                    ZONE ARCHITECTURE                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   ZONE A (Snowflake Postgres)     ZONE B (Snowflake)       │
│   ┌─────────────────────────┐     ┌─────────────────────┐  │
│   │ {Zone A objects}        │     │ {Zone B objects}    │  │
│   │                         │     │                     │  │
│   └─────────────────────────┘     └─────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Zone A Objects (if applicable)
| Object | Purpose | User Story |
|--------|---------|------------|
| {table} | {purpose} | [US#] |

### Zone B Objects (if applicable)
| Object | Purpose | User Story |
|--------|---------|------------|
| {table} | {purpose} | [US#] |

---

## Request Patterns

### Pattern Mapping

| Feature | Pattern | Zone(s) | User Story |
|---------|---------|---------|------------|
| {feature} | {A/B/C/D} | {A/B/Both} | [US#] |

### Pattern Details

#### Pattern {X}: {Name}
```
{ASCII diagram of data flow}
```

**Endpoints**:
- `{METHOD} /api/v1/{endpoint}` - {description}

---

## Cortex Configuration

### Features Required

```yaml
cortex_features:
  analyst:
    required: {true/false}
    implementation: "{inline/agent_object}"
    semantic_model: "specs/{demo-slug}/semantic-model.yaml"
    
  agent:
    required: {true/false}
    tools:
      - {tool_list}
    streaming: {true/false}
    
  search:
    required: {true/false}
    service_name: "{demo}_search_service"
    
  llm_functions:
    required: {true/false}
    use_cases:
      - "{use_case}"
```

### Semantic Model Location
- **Stage**: `@{database}.{schema}.STAGE/semantic-model.yaml`
- **Local**: `specs/{demo-slug}/semantic-model.yaml`

---

## Component Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   COMPONENT ARCHITECTURE                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  FRONTEND (React + Vite)                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Pages          Components        Services          │   │
│  │  ┌──────────┐   ┌──────────────┐  ┌─────────────┐  │   │
│  │  │ {pages}  │   │ {components} │  │ {services}  │  │   │
│  │  └──────────┘   └──────────────┘  └─────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                 │
│                           ▼                                 │
│  BACKEND (FastAPI)                                         │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Routers         Services          Connectors       │   │
│  │  ┌──────────┐   ┌──────────────┐  ┌─────────────┐  │   │
│  │  │ {routes} │   │ {services}   │  │ {connectors}│  │   │
│  │  └──────────┘   └──────────────┘  └─────────────┘  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Frontend Components

| Component | Purpose | User Story |
|-----------|---------|------------|
| {component} | {purpose} | [US#] |

### Backend Endpoints

| Method | Endpoint | Handler | User Story |
|--------|----------|---------|------------|
| {GET/POST} | `/api/v1/{path}` | `{handler}` | [US#] |

### Services

| Service | Responsibility | Zones |
|---------|---------------|-------|
| {service} | {responsibility} | {A/B/Both} |

---

## Database Schema

### Medallion Architecture

| Layer | Schema | Tables | Purpose |
|-------|--------|--------|---------|
| Bronze | `BRONZE` | {tables} | Raw data ingestion |
| Silver | `SILVER` | {tables} | Cleansed, conformed |
| Gold | `GOLD` | {tables} | Business-ready |

### Entity Relationship

```
{ERD diagram or relationship description}
```

---

## Multi-Tenancy (if applicable)

### Strategy: {Schema Isolation / Row-Level Security / Hybrid}

```yaml
multi_tenancy:
  enabled: {true/false}
  strategy: "{schema_isolation/rls/hybrid}"
  tenant_identifier: "{field_name}"
  isolation:
    zone_a: "{rls/schema}"
    zone_b: "{rls/schema}"
```

---

## Security Considerations

| Concern | Mitigation | Implementation |
|---------|------------|----------------|
| {concern} | {mitigation} | {how} |

---

## Constitution Check

| Principle | Status | Notes |
|-----------|--------|-------|
| Schema-First | {Pass/Fail} | {notes} |
| API-First | {Pass/Fail} | {notes} |
| Contract-First | {Pass/Fail} | {notes} |
| Spec-Driven | {Pass/Fail} | {notes} |
| Portable | {Pass/Fail} | {notes} |
| Demonstrable | {Pass/Fail} | {notes} |
| Minimal | {Pass/Fail} | {notes} |
| Testable | {Pass/Fail} | {notes} |
| Documented | {Pass/Fail} | {notes} |

---

## Next Steps

1. Run `/speckit.tasks` to generate implementation checklist
2. Review task dependencies and priorities
3. Begin implementation with Phase 1: Foundation

---

*Generated: {timestamp}*
